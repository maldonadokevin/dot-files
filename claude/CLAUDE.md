# Global preferences

## Code style

- Never write inline imports. All imports must go at the top of the file.
- Never remove comments (inline or block) as a side effect of an edit. If a comment is stale, update it. Only delete a comment when the task explicitly requires it.

## Git commits
Every git commit message should mention the corresponding JIRA ticket ID. If you don't know it, ask for it.
